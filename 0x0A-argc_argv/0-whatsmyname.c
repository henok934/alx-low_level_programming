#include <stdlib.h>
#include <stdio.h>

/**
 * main - functon must used for one ouput.
 *
 * @argc: returns number of character.
 * @argv: return the value of the character'
 *
 *return: returns the program.
 */

void main(int argc, char *argv[])
{
	(void)argc;
	(void)argv;
	printf("./mynameis\n");
	return (0);
}
